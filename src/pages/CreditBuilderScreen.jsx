import CreditBuilder from '../components/CreditBuilder/CreditBuilder';

const CreditBuilderScreen = () => {
   return (
      <div>
         <CreditBuilder />
      </div>
   );
};

export default CreditBuilderScreen;
