const Cards = ({ color }) => {
   return (
      <div className={`w-1/4 h-full bg-[${color}] border border-black`}>
         {' '}
         this is lagos{' '}
      </div>
   );
};

export default Cards;
